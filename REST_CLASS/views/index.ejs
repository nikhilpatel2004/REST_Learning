<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>All Posts</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <h1>Quora Posts</h1>

  <% for(let post of posts) { %>
    <div class="post">
      <h3 class="user">@<%= post.username %></h3>
      <h4 class="content"><%= post.content %></h4>
      <br>
      <a href="/posts/<%= post.id %>" class="btn btn-details">See in Details</a>
      <a href="/posts/<%= post.id %>/edit" class="btn btn-edit">Edit in Details</a>
      
 <form method="post" action="/posts/<%=post.id%>/delete?_method=DELETE" style="display:inline;">
          <button type="submit" class="btn btn-delete">Delete</button>
        </form>
        
    </div>
  <% } %>

  <br/>
  <br/>
  <a href="/posts/new" class="create-btn">Create New Post</a>
</body>
</html>
